<main>

  <section id="details-page">

    <div class="main_card">
      <div class="card_left">
        <div class="card_datails">

          <h1>Title: {{post.title}}</h1>
          <h3>Created by an author: {{post.authorName}}</h3>
          <div class="card_animal">
            <p class="card-keyword">Keyword: {{post.keyword}}</p>
            <p class="card-location">Location: {{post.location}}</p>
            <p class="card-date">Date: {{post.creationDate}}</p>
          </div>

          <p class="disc">Description: {{post.description}}</p>

          {{#if user}}
            <!-- If there is no registered user, do not display buttons-->
            <div class="social-btn">
              <!-- Only for registered user and author of the post -->
              {{#if post.isAuthor}}
                <a href="/post/edit/{{post._id}}" class="edit-btn">Edit</a>
                <a href="/post/delete/{{post._id}}" class="del-btn">Delete</a>
              {{else}}
                <!-- logged in users, who have not yet voted-->
                {{#if post.isUserVoted}}
                  <!-- logged in user who has already voted-->
                  <p class="thanks-for-vote">Thanks For Voting</p>
                {{else}}
                  <a href="/post/voteUp/{{post._id}}" class="vote-up">UpVote +1</a>
                  <a
                    href="/post/voteDown/{{post._id}}"
                    class="vote-down"
                  >DownVote -1</a>
                {{/if}}

              {{/if}}
            </div>
          {{/if}}

        </div>
      </div>
      <div class="card_right">
        <img src="{{post.image}}" alt="{{post.title}}" />
      </div>
    </div>

  </section>

  <section id="votes">
    <div class="vote-info">
      <div class="card_left">
        <div class="card_datails">
          <h1>Votes</h1>
          <div class="card_vote">
            <!-- Show the rating of votes, if there are no votes yet, the number must be 0. -->
            <p class="PV">Total rating of votes: {{post.postRating}}</p>
          </div>
          <!-- If there are already people who have cast their vote for the post, separate their emails with a comma and a space ", " -->
          {{#if post.votersEmails}}
            <p class="disc">People who voted for the post -
              {{post.votersEmails}}</p>
          {{else}}
            <!-- If not display: -->
            <p class="disc">People who voted for the post - No one has voted
              yet.</p>
          {{/if}}
        </div>
      </div>
    </div>
  </section>

</main>