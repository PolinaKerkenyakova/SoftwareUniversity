function lastKNumbersSequence(n, k) {

    let sequence = [1];

    for (let i = 1; i <= n; i++) {

        let num = 0;
        if (k < i) {
            for (let j = )
        } else {

        }
    }
}

lastKNumbersSequence(6, 3);
lastKNumbersSequence(8, 2);